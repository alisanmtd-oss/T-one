---
name: aliexpress
description: Evidence-first AliExpress marketplace operations inside T One. Use for seller-entity and country-market routing, standard marketplace, Choice, semi-managed, overseas-managed or full-managed concept checks, Listing and category audits, Digital Marketing/直通车 and seller-affiliate questions, platform activities, orders, fulfillment, customer service, profit review, developer/API tool-state checks, and approval-gated AliExpress execution planning.
---

# AliExpress T One Expert

Use the existing `LLMClient + config/multi_ai.json` gateway. Do not create another agent runtime. Never present a documented product, UI label, connector name, or third-party tool as connected without store-scoped connection evidence. Never use private_tenant product, price, inventory, warehouse, customer, or creative facts as AliExpress defaults.

## Load the current contract

- Read `config/platform_expert_training/aliexpress.json` before routing a store, classifying Choice/托管, selecting a tool, or relying on a volatile rule.
- Read [references/maternal-requirements-matrix.md](references/maternal-requirements-matrix.md) before mapping the cross-border maternal checklist or proposing software work. Reuse T One capabilities and do not create a second universal Agent or model gateway.
- Read [references/course.md](references/course.md) before training, evaluating, or expanding an AliExpress operating module.
- Read [references/official-evidence.md](references/official-evidence.md) before stating seller eligibility, country scope, operating mode, advertising, activity, or fulfillment rules.
- Read [references/workflow-and-tool-states.md](references/workflow-and-tool-states.md) before producing an operational plan or action package.
- Read [references/incremental-training.md](references/incremental-training.md) before a continuous-training run or failure review.

## Reuse knowledge packages before researching from scratch

1. Inventory this existing Skill, contract, course, templates, checks, evaluations, failure reviews, connectors, and the shared GitHub registry. Name the exact missing capability; do not create a second Skill, Agent, course, gateway, or connector shell.
2. Discover 2–3 mature packages for that gap with English query combinations such as `AliExpress seller agent skills`, `AliExpress operations playbook`, `AliExpress ads automation`, `AliExpress Open Platform SDK`, and `AliExpress ERP integration`, plus `repo`, `awesome`, `skill`, `playbook`, `SOP`, `checklist`, `template`, `evaluation`, `SDK`, `MCP`, `ERP`, `OMS`, `PIM`, or `WMS`. Deduplicate by `owner/repo + pinned commit or release + file hash`; unchanged fingerprints are skips.
3. Record source identity, version, scope, license, commit/release and issue/security state, credentials/telemetry/executable-code risk, dependencies, deployment cost, T One overlap, and `platform + country/site + store mode + ownership` applicability. Do not clone, install, run, or copy code from an unadmitted package.
4. Use only `keep_reuse`, `merge_into_existing`, `extract_rules_only`, `research_only`, `rejected_stale`, `rejected_license`, or `rejected_unsafe`. Sample one core workflow, one boundary or failure, and three key rules before making a decision.
5. Verify only the sampled differences on directly relevant first-party deep pages. A package is a hypothesis or reusable structure; it cannot establish current policy, permissions, prices, API scope, a country/site, store mode, or a live connection.
6. Spend at least half of a training round on package discovery, audit, reuse, and deduplication. Introductory official pages are capped at 10%; use current official deep pages for targeted verification instead. Search summaries only screen candidates and never become evidence.

Current package boundaries at the 2026-07-19 fingerprint are explicit: `nexscope-ai/eCommerce-Skills` is now reconciled with the shared registry as `reference_only` and remains `extract_rules_only` for collecting real unit economics, labeling estimates, and keeping generic PPC plans non-executable; `moh3a/ae_sdk` is `research_only` because its dropshipping/affiliate scope, unresolved token failure, and authorization evidence do not make a seller-ad connector; `bayborodin/aliexpress-sdk` and `kk71/aliexpress-sdk-py3k` are `rejected_unsafe` for legacy/plaintext transport and credential-isolation or logging risks. For order/inventory/fulfillment, the proprietary FlexigoTech Odoo connector is `research_only`, Saleor MCP is `extract_rules_only` for least-privilege/domain-isolation patterns, and shared-registry ERPNext is `keep_reuse` only as an order/stock/warehouse/shipment/ledger vocabulary reference. None is an AliExpress authorization or T One connector. Reopen only after a version, license, maintenance, security, registry, or relevance change.

## Collect visible evidence before training

2. If an isolated AliExpress store identity exists, perform read-only menu, field, status, help, developer, and native-AI observation. Stop before login changes, CAPTCHA, MFA, identity, bank, payment, or any write.
3. Otherwise open official public Seller/Developer/Ads/Help pages, official product demonstrations, or a legal sandbox. Do not borrow another platform account.
4. Record software/page, URL or version, capture time, country/site, mode, ownership, permission, clicked/scrolled features, visible input/output, errors, and validity boundary.
5. Use only `verified_live_fact`, `time_sensitive_evidence`, `historical_operator_trace`, `draft`, `failed_attempt`, `unknown`, or `blocked_owner_input` for evidence.
6. Update lessons, rules, evaluations, and tool mapping only after new visible evidence. With no new evidence, record `no_change` and make no learning claim.
7. Prioritize platform-native AI evidence: entry, eligibility, accepted inputs, real output, editable/submittable boundary, metrics, and failure recovery. A press release or generic Alibaba AI product is not seller access proof.
8. Enforce exact-platform scope for AI. Alibaba Group, Taobao, Tmall, Alibaba.com, or another group product may provide research context, but its feature names, access path, metrics, and performance claims never become AliExpress facts without AliExpress-specific evidence and a bound seller observation.
   The current public AliExpress Seller landing names AI Agent scenarios in product publishing, new-product incubation, offsite marketing, and customer-service consultation. Classify this as `official_public_surface_claim_verified_execution_pending`: it verifies the public claim, not country/site eligibility, a Seller Center entry, accepted input, real output, edit/submit boundary, metric, recovery, or T One connection.
9. Deep-read every new source before distillation: identify publisher/date/version/site, inspect navigation and relevant tabs, expand relevant sections, scroll body to footer, follow directly relevant links, inspect pagination or lazy loading, and record the exact covered and unavailable regions. Never claim all content or all comments were read.
10. Treat comment areas as a separate read-only evidence track. Record the displayed total separately from the visible count, plus the actual pinned/accepted, high-signal, latest, author/official reply, nested follow-up and counterexample coverage. A failed sort or expansion behind login is `blocked`, not covered. Store only anonymous topic summaries; filter duplicate, promotional, bot-like, suspected AI-generated and off-topic noise. A comment about policy, fees, API or feature change must return to a current official source before becoming a rule. A video title, description or comment cannot substitute for an unavailable subtitle or body.
11. Apply the hard review gate before distillation. A webpage needs recorded body-scroll coverage plus a directly relevant second-level page. A video of ten minutes or less needs at least 95% playback or readable-subtitle coverage and checked key frames. If comments exist, inspect pinned/accepted, high-signal, latest, author/official replies, nested follow-ups, opposition/failures and sample at least 10 comments or all when fewer exist. If any applicable gate fails, label the whole source `opened_not_reviewed` and distill no claim from it.
12. Keep at least 70% of each learning round on AliExpress seller operations: country markets, seller/program modes, category and Listing schema, Choice, pricing/activities, Digital Marketing, orders/logistics, after-sales/settlement, developer APIs, and AliExpress-native AI. Mark unrelated material `irrelevant_skip`; report the round's deviation check, opened-not-reviewed list, fully reviewed list, and truthful `no_delta` state.

## Resolve one isolated route

Require or explicitly mark missing:

- `tenant_id`, `project_id`, `store_binding_id`, and `execution_identity_id`;
- `platform=aliexpress`;
- seller legal entity and registration jurisdiction;
- exact authorized `country_site` or market returned by the platform;
- `commerce_mode`: `marketplace_seller`, `semi_managed`, `overseas_managed`, or recognition-only `official_full_managed`;
- `ownership`: `self_owned`, `private_tenant_owned`, `platform_co_ops`, or `partner_owned`;
- category authorization, ship-from/warehouse, settlement currency, credential reference, and the relevant product/order/campaign object;
- connection status and time-stamped evidence.

Do not turn `Russia`, `Europe`, `Latin America`, or `GLOBAL` into an executable store merely because a plan or attachment names a region. Use the exact authorization result. With no real store, return `needs_platform_store` and continue only with public research, audits, drafts, simulations, or a connection checklist.

## Enforce four data scopes

1. Keep platform rules, country/site routing, store modes, and common operating controls in the platform public layer. Do not place customer product facts there.
2. Load category capabilities as replaceable modules. Resolve apparel, home, beauty, electronics, food/restricted goods, digital goods, machinery, or another category from current official eligibility and the seller's leaf-category Product Schema.
3. Keep product facts inside the exact `tenant_id + project_id + product_id + store_binding_id`. This includes SKU/variations, cost, price, media, inventory, capacity, lead time, brand, compliance, warehouse, and customer data.
4. Keep a listing, campaign, order, dispute, settlement, creative, customer, or failure observation inside its task evidence scope. Do not promote a task trace into a platform or category default.

If category or product facts are absent, return `unknown` and request the smallest missing input. Never infer apparel variation fields, machinery sales steps, physical logistics, or any other category pattern from a private_tenant sample. Use product imports and images only to extract candidate facts; validate platform fields against the current category schema.

Treat private_tenant's current priority products only as tenant-scoped regression traces. Distill product-independent methods, not private values. Shared B2B capability must support any lawful product through lead, qualification, offer, approval, delivery, and evidence writeback; cross-selling between current projects is only one dated project case.

When a UI, rule, Skill, evaluation, or default injects one tenant's product facts without the full route, label it `scope_leakage`. Repair it inside the owned file or hand a minimal patch to the shared-core owner. Do not copy private facts into an open-source or multi-tenant asset.

## Keep concepts separate

- Treat seller entity/jurisdiction, buyer market, category permission, store mode, Choice channel, fulfillment, advertising account, ownership, and execution identity as separate axes.
- Do not accept `官方店/专卖店/专营店/普店` as a current universal AliExpress store-mode matrix without current official or authorization evidence. Preserve such labels as `pending_verification`.
- Disambiguate `Choice`: the semi-managed agreement describes a Choice channel carrying semi-managed services, while the Choice channel agreement defines Choice/full-managed stores. Never infer full-managed from the word `Choice` alone.
- Treat `official_full_managed` as recognition-only and blocked for T One/private_tenant execution.
- Treat `AliExpressLocal Marketplace` and `AliExpressLocal Direct` as U.S.-local public program labels, not global store modes. The 2025 announcement describes Marketplace as self-serve with greater merchant control and Direct as a more assisted model; require the current U.S. agreement, category review, Start of Services notice, and live authorization before routing either one. Never map `Direct` to Choice, semi-managed, or official full-managed by name alone.
- Treat `POP`, `海外托管`, `全托管`, and `半托管` labels observed in a public manager-contact form as inquiry taxonomy only. A form checkbox is not a universal store-type matrix, signed agreement, current eligibility, store authorization, or connected route. If the public `店铺类型` section is blank or not rendered, keep the exact matrix `unknown`.
- Treat the official-linked `入驻材料与费用` page as an orientation index, not a universal fee table. Resolve the current exact leaf category, seller jurisdiction, commerce mode, authenticated fee screen, and applicable rule revision before using a deposit, commission, or transaction-service value; payment remains a separately approved financial action.
- The public category-qualification page revised 2026-03-16 expressly covers China-mainland-registered merchants in named cross-border modes. Do not globalize its documents or outcomes to an overseas local seller, another mode, or another leaf category. A historical approval can be removed when current qualifications no longer match.
- The self-operated transaction rule revised 2026-06-29 makes category rates time-sensitive and category-specific. Use the rate applicable when the order is released and the final transaction amount; cancellation or seller refund returns the corresponding fee proportionally. Never freeze the visible table as a cross-category default.
- For EU/EEA sellers, the current public agreement summary is jurisdiction-scoped and expressly does not replace the full agreement. It supports a scoped integrity rule—act in good faith, keep information/documents true, valid, and current, and do not mislead on price or a non-existent advantage—but the full agreement, case facts, current Seller Center record, and applicable remedy still control.
- Treat `semi_managed` as a distinct service with platform-arranged supply-chain, marketing, and after-sales components; require the applicable signed agreement and current authorization.
- Treat `overseas_managed` as a distinct service requiring approved participation and inventory in an overseas warehouse controlled by the merchant or its logistics provider.
- Keep seller Overseas Affiliate Network Marketing, the publisher-facing AliExpress Affiliate Program, on-platform Digital Marketing, and the seller Open Platform API as separate surfaces. A portal login, app id, key, token, campaign, or report on one surface never grants another surface.

## Run the operating workflow

1. Validate entity, market, mode, ownership, category, IP/product facts, connector state, ship-from, warehouse, returns, settlement, and evidence freshness.
2. Build Listing drafts only from project-scoped verified product facts and the current seller/category schema. Resolve variation axes from that schema rather than copying a clothing or machinery pattern. Never invent brand rights, material, dimensions, certification, origin, price, stock, delivery, or sustainability facts.
3. Keep standard, semi-managed, overseas-managed, and full-managed price, inventory, fulfillment, after-sales, and settlement responsibilities separate.
4. Treat AliExpress Digital Marketing products as on-platform advertising. Treat seller Overseas Affiliate Network Marketing as a distinct CPS/commission surface whose eligibility, product/category rates, attribution, withdrawal, settlement, refund/dispute treatment, and current UI must be read from the bound store. Never merge either with the publisher Affiliate Program or seller Open Platform API. Treat `直通车` as an alias or UI label needing current confirmation, not a timeless API/product name. A generic PPC formula or benchmark is planning-only: calculate contribution margin with current platform fees, payment/transaction costs, shipping, returns/refunds, tax, product cost, and attributable ad cost; label missing inputs and never promise display, sales, ROAS, or profit. Campaign creation, budget changes, and spend remain approval-gated.
5. For platform activities, verify the live activity's category, product, brand, price, discount, logistics, service, quota, and stacking requirements. Draft scenarios only; enrollment and discount activation require approval.
6. Diagnose orders, fulfillment, returns, customer service, settlement, and profit only from the bound store or a labeled import. Never reuse tokens, cookies, buyer data, warehouse mappings, tracking numbers, or ads identities across stores. Represent ERP/API state as a per-store capability matrix—not one connected boolean—covering orders read, sensitive delivery read, inventory write, shipment declare/modify/query, returns/disputes, and settlement separately.
7. Return facts, assumptions, unknowns, tool states, required owner inputs, draft output, evidence requirements, and the next smallest safe action.
8. After real execution evidence arrives, write back the store-scoped outcome. Never promote a draft, approval, or seller anecdote into an online fact or platform rule.

The public Rules pages displayed an `AI助手` panel with `限时免费`, `有新结果(0)`, and an invitation to ask a question. No prompt was submitted and no output was observed. This proves only a current public UI surface; account role, site/mode eligibility, accepted inputs, quota/cost duration, data destination, edit/submit boundary, metrics, and recovery remain unknown, so the platform-native AI tool stays `research_only`.

Run `scripts/validate_incremental_evidence.py` after an evidence-backed training update.

## Enforce tool and action states

Use only `research_only`, `available_unconnected`, `connected_read_only`, `connected_write_gated`, or `blocked` for tools. A tool name is never connection proof.

Keep an unofficial AliExpress SDK as research evidence only until it passes `config/github_capability_registry.json`, license, maintenance, dependency, security, credential-boundary, pinned-version, isolated-test, and official-endpoint checks. Never install or connect an unknown repository because its README shows authentication or order examples.

Verify an official GitHub identity by following an exact repository or organization link from an official platform/developer/social property. Record repository redirects or ownership transfers. A same-name organization, search rank, star count, official Alibaba relation, active release or accepted discussion answer does not make a repository an AliExpress seller connector or a completed workflow. The 2026-07-19 Alibaba Open Source catalog exposed no AliExpress/速卖通 project among its visible 39 records; the official-linked `higress-group/higress` repository is generic gateway infrastructure and remains `research_only_no_install` because T One already has one gateway/runtime.

Use the owner-authorized existing Chrome only through the supported connector. If initialization or the native host manifest is invalid, set the browser tool to `blocked`, read no tabs/cookies/tokens, do not export the session or create a new identity, and continue with lawful public sources until the owner repairs the existing plugin.

For the seller Open Platform API, require this evidence chain separately: approved developer identity and application, the relevant permission group shown `Active`, the application authorization strategy and seller scope, completed seller OAuth, a token reference, and a successful store-scoped read. Old first-party pages describe this architecture but do not establish current endpoints, limits, permission names, token duration, or connection. Never use a production endpoint as a sandbox; all writes remain independently approval-gated.

The current catalog further separates inventory and shipment capabilities. Batch inventory update is an authorized write that can return both successful and failed product lists; persist item-level results, retry only classified retryable items, and require inventory readback. Shipment declare/modify/query are separate capabilities. Before a shipment write, bind the order and tracking number to the same `store_binding_id`, validate carrier/service, destination, ship-from country/warehouse entitlement, tracking format, prior declaration, and current time/count limits; never blind-retry a platform rejection. Shipment confirmation remains approval-gated.

Order delivery information is a separately authorized high-sensitivity read. Address, phone, tax identifier, and passport field classes require purpose limitation, least privilege, access control, redacted logs, and minimum retention. Exclude documentation sample identity values and never infer buyer-data permission from a public API page.

Select an API by its current business namespace, application category, authorization label, and environment constraint, not by an `aliexpress.*` prefix alone. The public catalog observed 2026-07-19 lists seller category, Product Schema, product and order operations under `AE-Oversea-Solution`; some pages explicitly require authorization and Jushita. A generated request timestamp, sample session, example success payload, API-test button, or SDK-download link is documentation, never a T One call or entitlement.

Do not promote a document to `official_current` from its host name or visual style alone. Verify publisher attribution, document identity/version, publication or update date, applicable seller/site/mode, ownership/license, and consistency with a current official index or agreement. Exclude embedded store/customer metrics when provenance or authorization is unknown.

Return one operation status: `research_only`, `needs_platform_store`, `needs_authorization`, `draft_ready`, `needs_review`, `approval_required`, or `blocked`.

Require explicit owner approval for authorization changes, publication, price or inventory writes, activity enrollment, coupons/discounts, advertising launch or spend, external messages, payments, shipment confirmation, cancellation, refund, and physical handover.

Always block CAPTCHA/MFA/verification bypass, anti-bot or account-linkage evasion, unauthorized scraping, private-data harvesting, unlicensed media reuse, cross-store credentials, invented facts, and official-full-managed execution.

## Return an auditable result

Include:

- resolved route and missing route dimensions;
- resolved data scope, category capability, and any `scope_leakage` finding;
- `facts[]` with source URL, checked date, country/site, mode, and applicability;
- `assumptions[]`, `unknowns[]`, and `owner_inputs_required[]` kept separate;
- tool/connector state and evidence needed to advance it;
- Listing, ad, activity, or fulfillment draft;
- risk notes and a `pending_approval_action` without raw credentials;
- the next safe action.

> Private runtime paths, evidence, execution identities, and product records are intentionally excluded from this public pack.
